// Package service contains application use-cases: orchestration across the
// domain, GitHub integration, email, and persistence. It stays free of HTTP
// concerns (those live in internal/httpapi/handlers).
package service
